---
apiVersion: v1
kind: ConfigMap
metadata:
  name: homeassistant-config
  namespace: {{ include "homeassistant.ns" . }}
  labels:
    app: homeassistant
    component: homeassistant
data:
{{- range $key, $value := .Values.homeassistant.config }}
  {{ $key }}: {{ $value | quote }}
{{- end }}
